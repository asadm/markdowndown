{
  "name": "cfworker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^0.0.6",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "vitest": "1.3.0",
    "wrangler": "^3.0.0"
  }
}
